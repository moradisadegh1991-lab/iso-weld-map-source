# بک‌لاگ اجرایی فاز ۱
## از ابزار تک‌کاربره به سامانهٔ «برج کنترل جوش و اسپول»

> سند همراه: `00-research-and-roadmap.md` · `02-reference-architecture.md`
> دامنه: ۳ ماه · یک واحد پایلوت (~۵۰۰ ایزومتریک)

---

## ۱. نقطهٔ شروع: آنچه امروز داریم

| فایل | خطوط | وضعیت | نقش در فاز ۱ |
|---|---|---|---|
| `lib/engine.js` | ۳۶۷ | ✅ کار می‌کند | **موتور قطعی — سرمایهٔ اصلی.** گراف، جوش، اسپول، اعتبارسنجی |
| `lib/standards.js` | ۷۰ | ✅ | جدول B16.9 / B36.10M |
| `lib/bom.js` | ۱۲۲ | ✅ | دروازهٔ MTO |
| `lib/excel.js` | ۱۲۳ | ✅ | خروجی پنج‌شیتی |
| `app/api/extract/route.js` | ۲۹۷ | ⚠️ | استخراج چندپاسه — باید پشت adapter برود |
| `app/page.jsx` | ۵۹۴ | ⚠️ | UI تک‌صفحه‌ای stateless — باید شکسته شود |
| `components/Viewer3D.jsx` | ۲۶۸ | ✅ | نمای سه‌بعدی |
| `components/WeldMap2D.jsx` | ۲۰۲ | ✅ | نقشهٔ دوبعدی SVG |

**شکاف اصلی:** سامانه **هیچ حافظه‌ای ندارد**. هر بار که مرورگر بسته می‌شود،
کل کار از بین می‌رود. این اولین چیزی است که باید حل شود.

---

## ۲. Epicها

### EPIC-1 · پایداری داده و چندکاربری — 🟢 لایهٔ داده پیاده شد
**چرا اول:** بدون این، هیچ‌کدام از قابلیت‌های دیگر معنا ندارند.

| Story | شرح | وضعیت |
|---|---|---|
| 1.1 | PostgreSQL + اسکیمای هسته | ✅ `db/migrations/001_core.sql` · runner با گارد checksum · `npm run db:migrate` |
| 1.2 | ذخیرهٔ فایل خام با hash | ✅ ذخیره‌سازی محتوا‌محور (SHA-256) · درایور local تست‌شده · درایور S3/MinIO نوشته، به bucket وصل نشده |
| 1.3 | پایدارسازی `extraction_run` | ✅ payload، `validation_checks` دست‌نخورده، توکن‌ها، مدل، زنجیرهٔ ممیزی تا بایت سند |
| 1.4 | احراز هویت + نقش‌ها | 🟡 **مجوزدهی** کامل و تست‌شده (`lib/authz.mjs`) · **احراز هویت** یک seam است؛ verifier واقعی به سرور Keycloak نیاز دارد |
| 1.5 | پروژه و واحد | ✅ با **Row Level Security** در خود پایگاه داده، نه `WHERE` در اپلیکیشن · نقش `app_rw` تا superuser آن را دور نزند |

**۲۱ تست روی PostgreSQL واقعی** (PGlite in-process) — همان migration، همان
سیاست‌های RLS که در production مستقر می‌شود. `npm run db:test`.

**باقی‌مانده:** سیم‌کشی به `app/page.jsx`. آن فایل یک کامپوننت ۵۹۴ خطی است و
سیم‌کشی پایداری با شکستنش یک کار است. جزئیات در [`db/README.md`](../../db/README.md).

---

### EPIC-2 · شناسهٔ پایدار جوش و کنترل رویژن (C-02، C-04)
**چرا:** پرارزش‌ترین قابلیت کل فاز ۱.

| Story | شرح | معیار پذیرش |
|---|---|---|
| 2.1 | تولید `weld_uid` پایدار از `(line_no, joint_kind, موقعیت روی مسیر)` | شمارهٔ جوش در رویژن جدید عوض نمی‌شود اگر جوش عوض نشده باشد |
| 2.2 | استخراج `revision` و `revision_date` از title block | دقت ≥ ۹۸٪ روی دیتاست طلایی |
| 2.3 | تشخیص رویژن جدیدتر و علامت‌گذاری `superseded_by` | نقشهٔ قدیمی از جریان کاری خارج و قفل می‌شود |
| 2.4 | موتور diff رجیستر بین دو رویژن | خروجی: `added / removed / changed` به‌ازای هر جوش |
| 2.5 | هشدار اسپول ساخته‌شده که رویژن جدید تغییرش داده | اعلان به کارگاه ساخت با فهرست اسپول‌های متأثر |

---

### EPIC-3 · ردیابی اجرای جوش (C-05، C-06)

| Story | شرح | معیار پذیرش |
|---|---|---|
| 3.1 | مدل و UI جوشکار + WQT (فرایند، دامنهٔ تأیید، انقضا) | |
| 3.2 | تخصیص جوش به جوشکار و WPS | |
| 3.3 | **اعتبارسنجی قطعی WQT:** آیا جوشکار مجاز به این قطر/ضخامت/فرایند است؟ | جوش خارج از دامنهٔ تأیید ⇒ خطای مسدودکننده، نه هشدار |
| 3.4 | هشدار WQT منقضی‌شده (و نزدیک به انقضا: ۳۰ روز) | |
| 3.5 | ثبت نتیجهٔ NDT و چرخهٔ تعمیر | نرخ تعمیر به تفکیک جوشکار قابل محاسبه شود |
| 3.6 | ثبت PWHT با ارجاع به گزارش | |

---

### EPIC-4 · موتور NDT (C-03)

| Story | شرح | معیار پذیرش |
|---|---|---|
| 4.1 | مدل Piping Class پروژه (قابل بارگذاری) | |
| 4.2 | موتور تعیین درصد و روش NDT: B31.3 §341.4 + حاکمیت Piping Class | Piping Class همیشه بر پیش‌فرض کد غلبه می‌کند |
| 4.3 | انتخاب نمونهٔ تصادفی جوش برای RT با قاعدهٔ پوشش جوشکار | هر جوشکار حداقل در نمونه نمایندگی داشته باشد |
| 4.4 | ردیابی «پوشش NDT» به تفکیک خط و جوشکار | |

---

### EPIC-5 · حلقهٔ انسان در جریان (HITL)

| Story | شرح | معیار پذیرش |
|---|---|---|
| 5.1 | صف بازبینی: نقشه‌های در انتظار تأیید مهندس پایپینگ | مرتب‌سازی بر اساس تعداد WARN |
| 5.2 | UI ویرایش فیلدی با ثبت «قبل/بعد» | هر ویرایش لاگ می‌شود — همین داده، سوخت بهبود دقت است |
| 5.3 | امضای دیجیتال تأیید (کاربر، زمان، hash سند و رجیستر) | |
| 5.4 | قفل شدن رجیستر تأییدشده؛ تغییر بعدی نیاز به رویژن جدید | |
| 5.5 | چاپ disclaimer روی هر خروجی (اکسل، SVG، PDF) | متن فعلی README در UI هم دیده شود |

---

### EPIC-6 · لایهٔ انتزاع مدل و مهاجرت به خودمیزبان

| Story | شرح | معیار پذیرش |
|---|---|---|
| 6.1 | استخراج interface `llmClient` از `route.js` | متدها: `extract(pass, images, prompt, opts)` |
| 6.2 | پیاده‌سازی adapter `anthropic` (رفتار فعلی) | تست‌های موجود بدون تغییر رفتار پاس می‌شوند |
| 6.3 | پیاده‌سازی adapter `openai-compatible` برای vLLM | با یک متغیر محیطی جابه‌جا می‌شود |
| 6.4 | راه‌اندازی vLLM با Qwen3-32B + Qwen2.5-VL | endpoint داخلی پاسخ‌گو |
| 6.5 | اجرای eval روی هر دو مسیر و مقایسه | افت دقت < ۵٪ ⇒ سوییچ به داخلی |

---

### EPIC-7 · زیرساخت ارزیابی — ✅ پیاده‌سازی شد
**چرا این یک epic مستقل است:** تحقیق بازار روشن است — بیشترین برد production از
زیرساخت ارزیابی می‌آید، نه ارتقای مدل.

| Story | شرح | وضعیت |
|---|---|---|
| 7.1 | ساختار `eval/` با موارد طلایی و رجیستر مرجع | 🟡 ساختار + ۱ مورد طلایی واقعی + ۱۵ مورد قاعده‌ای. رساندن به ۵۰ نقشه، کار SME در فاز ۰ است |
| 7.2 | runner که موتور را روی کل مجموعه اجرا و متریک بدهد | ✅ `npm run eval` — exact-match جوش، مجموعهٔ Field، اسپول، دقت فیلدی سطربه‌سطر، نرخ WARN |
| 7.3 | اجرای eval در CI روی هر PR | ✅ `.github/workflows/eval.yml` + دروازهٔ `npm run eval:check` (با رگرسیون تزریقی تست شد: exit 1) |
| 7.4 | خروجی ماشین‌خوان برای روند دقت | ✅ `--json` · اتصال به Grafana/Langfuse با زیرساخت فاز ۱ |
| 7.5 | ثبت baseline اولیه قبل از هر تغییری | ✅ `eval/baseline.json` — ۱۴ pass · ۲ xfail · دقت فیلدی رجیستر ۱۰۰٪ |

**دستاورد جانبی:** هارنس در اولین اجرا دو باگ واقعی موتور را پیدا کرد
(شیر BW و شیر فلنجی) — ثبت‌شده در [`04-engine-findings.md`](04-engine-findings.md).

---

### EPIC-8 · گزارش‌دهی مدیریتی

| Story | شرح | معیار پذیرش |
|---|---|---|
| 8.1 | View‌های گزارشی در Postgres برای مصرف Power BI | |
| 8.2 | داشبورد: پیشرفت جوشکاری، نرخ تعمیر NDT، وضعیت اسپول، پوشش ITR | مخاطب: مدیر ساخت و سرپرست QC |
| 8.3 | حفظ خروجی اکسل موجود به‌عنوان کانال ثانویه | کاربران میدانی هنوز اکسل می‌خواهند — این را حذف نکنید |

---

## ۳. توالی سه‌ماهه

```
ماه ۱   EPIC-7 (baseline) → EPIC-1 (پایداری داده) → EPIC-6.1–6.3 (adapter)
ماه ۲   EPIC-2 (رویژن و weld_uid) → EPIC-5 (HITL) → EPIC-6.4–6.5 (vLLM)
ماه ۳   EPIC-3 (ردیابی اجرا) → EPIC-4 (NDT) → EPIC-8 (گزارش) → UAT
```

**نکتهٔ ترتیب:** EPIC-7 قبل از همه‌چیز. بدون baseline دقت، در ماه سوم نمی‌توانید
ثابت کنید چیزی بهتر شده است.

---

## ۴. بدهی فنی موجود که باید در این فاز تسویه شود

| مورد | فایل | اقدام |
|---|---|---|
| کامپوننت ۵۹۴ خطی تک‌فایل | `app/page.jsx` | شکستن به صفحات: آپلود، بازبینی، رجیستر، گزارش |
| ~~نبود تست خودکار~~ | ~~کل مخزن~~ | ✅ تسویه شد — `eval/` قواعد جوش را به‌صورت موردی قفل کرده است |
| کلید API در سمت کلاینت | `page.jsx` | حذف کامل؛ فقط سمت سرور با مدیریت راز |
| نبود مدیریت خطای ساخت‌یافته | `route.js` | کد خطای استاندارد به‌جای متن آزاد |
| وابستگی به `maxDuration` پلتفرم | `route.js` | انتقال به کار پس‌زمینه با صف — پردازش دسته‌ای ۵۰۰ نقشه با request/response نمی‌شود |
| نبود پردازش دسته‌ای | — | آپلود پوشه‌ای + صف — نیاز قطعی پایلوت |

---

## ۵. تعریف «انجام‌شده» برای فاز ۱

فاز ۱ وقتی تمام است که **همهٔ** این‌ها برقرار باشد:

- [ ] ۵۰۰ ایزومتریک واحد پایلوت پردازش و در پایگاه داده ذخیره شده‌اند
- [ ] دقت تعداد جوش روی دیتاست طلایی ≥ ۹۵٪ (exact match)
- [ ] مهندس پایپینگ حداقل ۱۰۰ رجیستر را در سامانه تأیید کرده است
- [ ] کنترل رویژن روی حداقل یک مورد واقعی تغییر نقشه عمل کرده و هشدار داده است
- [ ] هیچ جوشی بدون جوشکار دارای WQT معتبر ثبت نشده است
- [ ] داشبورد Power BI در جلسهٔ هفتگی ساخت استفاده می‌شود
- [ ] eval در CI اجرا می‌شود و روند دقت مستند است
- [ ] سامانه روی زیرساخت داخلی (vLLM) بدون وابستگی به API خارجی کار می‌کند
