# بررسی دوبارهٔ کل پروژه — آنچه ساخته شده و آنچه مانده

تاریخ بررسی: 2026-09-26 · شاخهٔ `claude/vigilant-brown-wnaksu` (PR #1) · بررسی قبلی:
[`01-ecosystem-review.md`](01-ecosystem-review.md) · تطبیق بند به بند با Master Plan:
[`00-master-plan-alignment.md`](00-master-plan-alignment.md).

## ۱. خلاصه

| | بررسی قبلی (09-25) | اکنون |
|---|---|---|
| کد برنامه (`lib`، `app`، `components`) | حدود ۲۱٬۵۰۰ خط | حدود ۳۰٬۷۰۰ خط |
| مایگریشن دیتابیس | ۲۱ | ۳۵ (همه با RLS روی جدول‌های پروژه‌ای) |
| تست دیتابیس | ۲۸ مجموعه، ۵۴۶ تست | **۴۱ مجموعه، ۶۹۳ تست — همه پاس** |
| موتور جوش (eval) | — | ۲۶ مورد: ۲۵ پاس، ۱ پیش‌نویس؛ بدون پسرفت |
| صفحه / API | ۲۱ بخش در منو | ۳۰ بخش فعال در ۷ گروه، ۳۱ صفحه، ۵۶ مسیر API |
| CI روی PR | — | سبز: `eval` و `data-layer` روی آخرین commit |

از بررسی قبلی تا امروز، ۸ یافته از ۱۱ بسته شد (یکی نیمه)، ۱۵ ماژول و قابلیت جدید اضافه شد و
یک باگ جدی گزارش‌گیری (F-15) پیدا و رفع شد. این بررسی **سه مورد تازه** پیدا کرد که
باید پیش از استفادهٔ واقعی رفع شوند (بخش ۴): نسخهٔ Next.js با آسیب‌پذیری‌های امنیتی
شناخته‌شده، نبود روال پشتیبان‌گیری دیتابیس، و رفتار گزارش‌ها وقتی استخراج رویژن
جدید یک نقشه شکست بخورد.

## ۲. کیفیت — چطور ادعاها اثبات شده‌اند

- **تست روی PostgreSQL واقعی** (PGlite، همان migrationهای production)، نه mock.
- **هر گارد با mutation امتحان شده:** کد عمداً خراب می‌شود و تست باید شکست بخورد. در دو
  دور آخر ۹ + ۱۷ mutation، همه گرفته شد (یک مورد زنده ماند و با تست تازه بسته شد).
- **هر ماژول یک drive در مرورگر واقعی دارد** (`tools/drive-*.mjs`) روی build تولیدی —
  چون «کامپایل شد» در این مخزن بارها یعنی هیچ.
- **یافته‌های موتور ثبت می‌شوند** (`docs/epc-llm/04-engine-findings.md`، F-10 تا F-15)،
  هرکدام با تستی که پیش از رفع شکست خورده.

## ۳. آنچه ساخته شده

### هسته و مشترک
- پروژه، واحد، پیمانکار و بستهٔ قرارداد؛ ستون فقرات **تگ ← ساب‌سیستم** که هر آیتم هر
  رشته با id به آن وصل است.
- شناسنامهٔ تجهیز (یک تگ، همهٔ رشته‌ها) و آمادگی دیجیتال.
- رجیستر فرضیات با رویژن و تصمیم امضاشده؛ رجیستر اطلاعات ناقص با رشتهٔ مسئول.
- ورود با رمز، محدودیت تلاش، ابطال نشست، پنهان‌سازی خطای داخلی.
- **کاربران و دسترسی:** دعوت با لینک یک‌بارمصرف، نقش، طرف بازرسی، **حوزهٔ کاری (رشته)** که
  منو و ثبت را محدود می‌کند؛ نقش توسعه‌دهنده با دسترسی کامل.
- رابط: تم روشن/تیره، منوی گروه‌بندی‌شده با جست‌وجو (Ctrl+K)، جدول‌ها با جست‌وجو، فیلتر،
  مرتب‌سازی و خروجی CSV، فرم‌های تاشو، کشوی منو روی گوشی.

### اجرای رشته‌ها
| رشته | ساخته شده | مرجع |
|---|---|---|
| پایپینگ | استخراج ایزومتریک با مدل + موتور قطعی، رجیستر جوش امضاشده با هش، رویژن با شناسهٔ پایدار جوش، جوشکار و صلاحیت، NDT با نمونه‌گیری بذردار، زنجیرهٔ اسپول، ساپورت، **سابقهٔ جوش (Joint History)** | ASME B31.3، ASME IX |
| تجهیزات | زنجیرهٔ پیش‌نیاز دوّار، ثابت، کوره | — |
| سیویل | فونداسیون، بتن‌ریزی، عمل‌آوری، پذیرش مقاومت | ACI 318 |
| سازه | شاقولی، پیش‌تنیدگی پیچ | AISC، RCSC |
| برق | لیست کابل، ترمینیشن، IR | IEC 60364-6 |
| ابزار دقیق | ایندکس، کالیبراسیون، لوپ چک | ISA 5.1 |
| رنگ و عایق | آماده‌سازی، نقطهٔ شبنم، DFT، نگه‌داشت عایق تا تست | ISO 8501-1، ISO 19840 |

### سایت
- برچسب QR برای تگ، اسپول، کابل و ابزار؛ اسکنر داخل برنامه.
- **PWA آفلاین:** بستهٔ سایت روی گوشی، صف ثبت با همگام‌سازی idempotent، پیش‌داوری با همان
  موتور؛ ثبت آفلاین کابل و ابزار؛ عکس سایت برای Punch.

### کیفیت، تکمیل و تحویل
- **بازرسی:** ITP سند کنترل‌شده با رویژن، نقاط H/W/R/S برای پیمانکار، کارفرما و TPI، درخواست
  بازرسی (IR) که مرحلهٔ زنجیره را تا آزادسازی قفل می‌کند؛ **رسید کالا (MIR/MTC) هم زیر ITP**.
- Punch A/B/C و NCR با تشدید؛ Test Pack با فشار تست B31.3؛ گواهی MC؛ پیش‌راه‌اندازی و RFSU.
- تحویل به نگهداری: شناسنامهٔ ISO 14224، FLOC، Criticality، خروجی.

### مدیریت پروژه
- کنترل پروژه: S-curve، EVM، دفتر هزینه، رجیستر ریسک.
- خرید: PO، Expediting، FAT، حمل، رسید سایت، VDRL.
- انبار: لات، MIR/MTC، حواله با مقصد، ردیابی ذوب، فراخوان، کسری نسبت به MTO.
- کنترل مدارک: رجیستر، رویژن با هدف صدور، ترانسمیتال.
- HSE: طبقهٔ رویداد IOGP، LTIF/TRIR، مجوز کار با تست گاز، مشاهدات.
- ذخیرهٔ فایل روی MinIO با کنترل هش؛ لایهٔ `reporting.*` برای Power BI.

## ۴. یافته‌های این بررسی

### ✅ R1 · Next.js 14.2.30 آسیب‌پذیری‌های امنیتی شناخته‌شده دارد — بحرانی (رفع شد)
`npm audit` برای نسخهٔ فعلی Next.js ده‌ها advisory گزارش می‌کند، از جمله اجرای کد از راه دور
در Image Optimization API (با فایل AVIF)، SSRF، cache poisoning و DoS در Server Components.
برنامه از `next/image` استفاده نمی‌کند، ولی endpoint بهینه‌ساز تصویر به‌طور پیش‌فرض باز است.

**پیشنهاد:** فوراً به `14.2.35` (آخرین وصلهٔ شاخهٔ ۱۴) بروید و بهینه‌ساز تصویر را خاموش کنید
(`images.unoptimized`). سپس ارتقا به نسخهٔ پشتیبانی‌شده (15.5 یا 16) در یک PR جدا، چون
هم باید PGlite زیر webpack و هم کامپایلر WASM روی Termux دوباره آزموده شوند.

**رفع (همان روز):** ارتقا به **Next 15.5.26** و React 19 — شاخهٔ ۱۴ فقط ۵ مورد را وصله کرده
بود و دو مورد بحرانی (RCE) تنها از 15.5.24 رفع شده‌اند. بهینه‌ساز تصویر خاموش شد
(`images.unoptimized`؛ `/_next/image` حالا ۴۰۴ است)، سرآیند `X-Powered-By` برداشته شد، پارامتر
مسیرهای API مطابق Next 15 await می‌شود، و `postcss`/`nanoid` داخل Next با `overrides` به نسخهٔ
وصله‌دار رفتند. پس از آن `npm audit` برای وابستگی‌های اجرایی فقط `xlsx` را نشان می‌دهد (R4).
اسکریپت Termux حالا نسخهٔ کامپایلر WASM را می‌سنجد و نسخهٔ قدیمی را جایگزین می‌کند. سنجش:
۴۲ مجموعه تست، eval، `next build`، `next dev`، و **هر ۳۰ drive مرورگر** روی build تولیدی (پنج drive
که از دور رابط کاربری کهنه شده بودند به‌روز شدند).

### ✅ R2 · روال پشتیبان‌گیری دیتابیس نوشته نشده — بالا (رفع شد)
برای فایل‌ها روال MinIO (`mc mirror`) هست، ولی برای **خود دیتابیس** نه `pg_dump` زمان‌بندی‌شده
و نه تمرین بازیابی مستند شده است. دیتابیس سوابق امضاشده (رجیستر جوش، MC، RFSU) را نگه
می‌دارد که از دست رفتنش جبران‌پذیر نیست.

**پیشنهاد:** پشتیبان روزانه، نگه‌داری خارج از سرور، و یک تمرین بازیابی ثبت‌شده در RUNBOOK.

**رفع (همان روز):** `npm run db:backup` و `npm run db:restore` (`lib/db/backup.mjs`). PostgreSQL با
`pg_dump` داخل یک snapshot که شمارش مانیفست هم در همان است؛ PGlite با tarball پوشهٔ داده، که
تا سرور زنده است (از روی pid واقعی‌اش) انجام نمی‌شود. هر پشتیبان پیش از اعلام خوانده و شمرده
می‌شود؛ بازیابی فقط در مقصد خالی و با اثبات شمارش ردیف‌ها؛ فایل دست‌کاری‌شده یا بی‌مانیفست
رد می‌شود؛ نگه‌داری N تای آخر. سنجیده شد روی PostgreSQL 16 واقعی: پشتیبان در حالی که نوشتن
ادامه داشت، بازیابی روی یک سرور تازه بدون نقش‌ها، و اجرای برنامه (ورود، RLS، ثبت) روی نسخهٔ
بازیابی‌شده. روال روزانه، کپی خارج از سرور و تمرین ماهانه در RUNBOOK. `db/test/backup.mjs`: ۱۱ تست
(بخش PostgreSQL با `BACKUP_TEST_DATABASE_URL`).

### R3 · اگر استخراج رویژن جدید شکست بخورد، نقشه از گزارش‌ها بیرون می‌رود — متوسط
پس از رفع F-15، فقط رجیستر رویژن در حال اجرا شمرده می‌شود. اگر رویژن جدید ثبت شود ولی
استخراجش شکست بخورد، آن نقشه در آن فاصله هیچ جوشی در گزارش ندارد. این «عدد کمتر» است،
نه «عدد غلط»، ولی بی‌صدا است.

**پیشنهاد:** این حالت در رجیستر «اطلاعات ناقص» بیاید: «رویژن در حال اجرای نقشهٔ X رجیستر
معتبر ندارد»، با رشتهٔ مسئول.

### R4 · کتابخانهٔ `xlsx` 0.18.5 — پایین
دو advisory بدون وصله روی npm (prototype pollution و ReDoS هنگام **خواندن** فایل). برنامه فقط
با آن فایل Excel **می‌سازد**، پس مسیر آسیب در دسترس نیست. `nanoid` (وابستگی غیرمستقیم)
با `npm audit fix` رفع می‌شود. **پیشنهاد:** جایگزینی با نسخهٔ رسمی SheetJS یا `exceljs` در
یک دور نگه‌داری.

### R5 · PR بسیار بزرگ — بالا (فرایندی)
PR #1 حالا ۷۰ commit و بیش از ۵۴ هزار خط دارد. CI سبز است. هر دور که ادغام عقب بیفتد،
بازبینی سخت‌تر می‌شود. **پیشنهاد:** ادغام، و از این پس یک PR برای هر ماژول.

### وضعیت یافته‌های بررسی قبلی

| یافته | وضعیت |
|---|---|
| F1 کندی موتور آمادگی | ✅ دسته‌ای شد |
| F2–F4 امنیت ورود، ابطال نشست، خطای ۵۰۰ | ✅ رفع شد |
| F5 حلقه‌های کیفیت و تحویل (Punch، NCR، MC، CMMS) | ✅ ساخته شد |
| F6 ورودی‌های بالادستی (خرید، مدارک) | ✅ ساخته شد |
| F7 شمارش پیشرفت سیویل، سازه و رنگ | ✅ ساخته شد — با همان موتور صفحه‌ها، به‌علاوهٔ اسنپ‌شات ماهانهٔ EV |
| F8 تحویل به CMMS | ✅ مرحلهٔ ۱ و ۲ (برنامهٔ PM، SPIR، کالیبراسیون دوره‌ای) ساخته شد |
| F9 ورود داده در سایت (QR، آفلاین) | ✅ ساخته شد |
| F10 PR بزرگ | ⬜ باز (R5) |
| F11 «بازرسی‌شده» ≠ «پذیرفته» | ✅ رفع شد |
| F-15 رویژن منسوخ در گزارش‌ها دو بار شمرده می‌شد | ✅ رفع شد (در این دور پیدا شد) |
| F-16 جوش ردشده در EV «تست‌شده» بود | ✅ رفع شد (هنگام ساخت F7 پیدا شد) |

## ۵. آنچه مانده — به ترتیب پیشنهادی

### الف) فوری، پیش از استفادهٔ واقعی
1. ~~ارتقای Next.js و خاموش کردن بهینه‌ساز تصویر (R1).~~ ✅
2. ~~پشتیبان‌گیری دیتابیس و تمرین بازیابی (R2).~~ ✅ — فقط زمان‌بندی روی سرور شما و اولین تمرین بازیابی مانده.
3. ادغام PR #1 (R5).

### ب) کامل کردن داده‌ها — کوتاه‌مدت
4. ✅ (ساخته شد، ۲۰۲۶-۰۹-۲۶) شمارش سیویل، سازه و رنگ برای EV پلتفرم، و اسنپ‌شات ماهانهٔ EV (F7).
   شمارش با همان موتوری است که صفحهٔ هر رشته را می‌سازد (نه بازنویسی قواعد در SQL)؛ تاریخ
   گذشته از آخرین اسنپ‌شات خوانده می‌شود. در راه F-16 هم پیدا و رفع شد.
5. ✅ (ساخته شد، ۲۰۲۶-۰۹-۲۶) **CMMS مرحلهٔ ۲:** برنامهٔ PM (از دستورالعمل سازنده یا RCM، بدون حدس بازه)، قطعات یدکی
   (SPIR: راه‌اندازی، دوساله، بیمه‌ای) متصل به کالای انبار، کالیبراسیون دوره‌ای ابزار دقیق.
6. ✅ (ساخته شد، ۲۰۲۶-۰۹-۲۶) **خرید مرحلهٔ ۲:** MR با رویژن، مناقصه و ارزیابی (بدون حدس نرخ ارز)، واگذاری → PO، و Vendor Data Template که به شناسنامهٔ نگهداری می‌رود.
7. ITP رسید کالا به تفکیک دسته (مواد تحت فشار، الکتریکال)؛ هشدار R3 در اطلاعات ناقص.
8. ✅ (ساخته شد، ۲۰۲۶-۰۹-۲۶) انبار: رزرو مواد برای اسپول/تگ/مصرف (مانده از حواله‌ها محاسبه می‌شود، رزروِ دیگری حواله نمی‌شود)، انبارگردانی با مقایسه با دفتر همان روز و تصمیم نفر دوم (اصلاح یا حفظ دفتر)، و کد MESC از کاتالوگ کارفرما. رسید در برابر PO از قبل بود.
9. ✅ (ساخته شد، ۲۰۲۶-۰۹-۲۶) کنترل مدارک: ترانسمیتال ورودی از طراح با فایل هر رویژن روی MinIO (با هش؛ فایل تکراری رد)، مهلت پاسخ از مهلت قراردادی (بدون پیش‌فرض)، توزیع بین رشته‌ها (IDC)، کامنت‌هایی که با مدرک از رویژنی به رویژن دیگر می‌روند تا پاسخ طراح بسته شوند (CRS)، و ترانسمیتال پاسخ با کدی که با سابقه بخواند.
10. HSE: JSA/TRA، بازرسی داربست و جرثقیل، آموزش و کارت صلاحیت افراد.
11. Asset Master: شرایط طراحی از دیتاشیت، شناسه‌های DCS/Historian.

### ج) بخش‌های Master Plan که شروع نشده‌اند
12. خواندن P&ID و SLD با همان الگوی ایزومتریک (مدل می‌خواند، موتور تصمیم می‌گیرد، انسان امضا می‌کند).
13. راه‌اندازی پس از RFSU، RFC جدا، Performance Test.
14. خاکبرداری و داربست با تفکیک «Preliminary / Calculated / Survey-Verified».
15. فاز بهره‌برداری: Historian/OPC UA از طریق DMZ، انرژی، Production Accounting.
16. RAG با قاعدهٔ «بدون منبع، بدون جواب».

### د) وابسته به داده یا تصمیم بیرون از کد
- **دقت استخراج روی نقشه‌های واقعی** هنوز اندازه‌گیری نشده: به نقشه‌های پروژه همراه با
  رجیستر پیمانکار (حقیقت مرجع) نیاز است. عدد eval فقط دقت موتور است، نه دقت استخراج.
- Keycloak/SSO: درزش در کد آماده است؛ به سرور هویت سازمان نیاز دارد.

## ۶. جمع‌بندی
هستهٔ پلتفرم — ستون فقرات مشترک، موتورهای قطعی، امضای انسانی، سوابق غیرقابل‌ویرایش و
جداسازی پروژه در دیتابیس — محکم است و با ۶۹۳ تست و CI سبز پشتیبانی می‌شود. تقریباً همهٔ
زنجیرهٔ ساخت، از رسید کالا تا RFSU و تحویل به نگهداری، ساخته شده است. پیش از استفادهٔ
واقعی روی سرور، سه کار الف واجب‌اند. بعد، بیشترین ارزش در کامل کردن داده‌های EVM و CMMS
مرحلهٔ ۲ است.
