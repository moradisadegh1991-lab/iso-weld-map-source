# Digital EPC Master Plan — نقشهٔ تطبیق با آنچه ساخته شده

مجتمع اتیلن ۵۰۰ هزار تن در سال، خوراک اتان، Utilities داخلی، ذخیره‌سازی و صادرات.

این سند Master Plan کارفرما/مشاور را بخش‌به‌بخش با پلتفرم فعلی مقایسه می‌کند:
چه چیزی **ساخته و آزموده شده**، چه چیزی **نیمه‌کاره** است و چه چیزی **شروع نشده**.
هر ادعای «ساخته شده» به یک فایل و یک تست اشاره می‌کند؛ چیزی که تست ندارد
ساخته‌شده حساب نمی‌شود.

قواعد حاکم Master Plan (Data First، یک Asset یک شناسه، UNKNOWN به‌جای حدس،
Assumption Register، امضای انسانی، AI فقط Decision Support، سیستم موجود اول)
در `.claude/skills/epc-platform/SKILL.md` به قواعد کار این مخزن اضافه شده‌اند.

---

## ۱. آنچه با Master Plan هم‌راستاست و ساخته شده

| بخش Master Plan | در پلتفرم | فایل / تست |
|---|---|---|
| §3 یک Asset ID واحد، §55 Digital Thread | ستون فقرات `tag` / `subsystem`؛ هر آیتم هر رشته با **id** به تگ وصل است، نه با کپی رشتهٔ تگ | `007_spine.sql`، `db/test/spine.mjs` |
| §5 سلسله‌مراتب | پروژه → واحد (با تراز گرید اختصاصی) → سیستم/ساب‌سیستم → تگ | `009`، `012` |
| §7، §11 (C-31) Isometric → Joint Register | استخراج با مدل + موتور قطعی B31.3، رجیستر جوش امضاشده با هش و رویژن | `lib/engine.js`، `db/test/run.mjs` |
| §12 Joint History، §13 Welder | جوشکار، صلاحیت، ثبت جوش، NDT، تعمیر؛ سیکل تعمیر. **صفحهٔ سابقهٔ جوش** (`/piping/joint`): هر جوش با شناسهٔ پایدارش در همهٔ رویژن‌های نقشه (شماره و Shop/Field هر رویژن و تغییرها)، مواد و شمارهٔ ذوب حواله‌شده به اسپولش، فیت‌آپ و IRهای اسپول، جوشکار و WPS و حکم صلاحیت همان زمان، نمونهٔ NDT و هر شات و تعمیر، PWHT، تست فشار خط و اصلاحات استخراج نقشه — روی یک خط زمانی؛ و آنچه با هم نمی‌خواند (جوش بعد از تست پذیرفته، ذوب با MTC رد، شات رد بی‌شات بعدی، نمونهٔ شات‌نشده، جوش حذف‌شده در رویژن جاری ولی زده‌شده). رفع F-15 همین‌جا پیدا شد | `003`، `035`، `lib/db/repos/joint.mjs`، `db/test/execution.mjs`، `db/test/joint.mjs` |
| C17 Spool Tracking | زنجیرهٔ اسپول: صدور → فیت‌آپ → جوش کارگاهی* → NDT* → ارکشن → جوش سایت* → ساپورت → تست → رنگ و عایق* → تحویل (*از داده خوانده می‌شود) | `precedence.mjs`، `piping-execution.mjs` |
| §21 MC readiness (پایه) | موتور پیش‌نیاز: «چه کاری امروز شروع می‌شود» و «چرا آماده نیست» تا ریشه، برای هر تگ | `db/test/precedence.mjs` |
| C41 فونداسیون و بتن | فونداسیون روی ستون فقرات؛ پذیرش مقاومت ACI 318؛ فونداسیون، مرحلهٔ «فونداسیون» تجهیز و سازه را جواب می‌دهد | `lib/civil/concrete.mjs`، `db/test/civil.mjs` |
| سازهٔ فلزی | شاقولی AISC 303 / EN 1090-2؛ پیش‌تنیدگی پیچ AISC 360 / RCSC / EN 1090-2 | `lib/structural/steel.mjs` |
| §10، C-33 لیست کابل | ورود لیست کابل با قاعده؛ IR طبق IEC 60364-6؛ مرحلهٔ «برق» تجهیز از کابل‌هایش | `lib/electrical/cable.mjs` |
| §9، C-32 Instrument Index | خواندن تگ با جدول ISA 5.1؛ لوپ؛ کالیبراسیون؛ لوپ چک یک‌بار برای لوپ | `lib/instrumentation/isa.mjs` |
| رنگ و عایق | ISO 8501-1، نقطهٔ شبنم، ISO 19840، نگه‌داشت عایق تا تست (B31.3 §345.3.1) | `lib/coating/coating.mjs` |
| §14 Material Traceability، انبار | لات = یک ردیف رسید (کالا، Heat، MTC)؛ موجودی = پذیرفته − حواله + برگشت، هرگز ذخیره نمی‌شود؛ مواد ردیابی‌پذیر فقط با Heat و MTC پذیرفته حواله می‌شوند؛ هر حواله مقصد (اسپول/تگ) دارد و رد دیرهنگام MTC فهرست فراخوان می‌دهد؛ کسری نسبت به MTO بدون احتساب قرنطینه | `lib/warehouse/stock.mjs`، `019`، `/warehouse`، `db/test/warehouse.mjs` |
| HSE | طبقهٔ رویداد (FAT/LWC/RWC/MTC/FAC/NM، IOGP) از واقعیت‌ها مشتق می‌شود نه انتخاب؛ LTIF/TRIR هم با پایهٔ ۱٬۰۰۰٬۰۰۰ (IOGP) هم ۲۰۰٬۰۰۰ (OSHA)؛ مجوز کار گرم و فضای بسته فقط با تست گاز تازه و قبول در برابر حدود اعلام‌شدهٔ پروژه، و صادرکننده ≠ درخواست‌کننده؛ انقضا محاسبه‌ای؛ SIMOPS | `lib/hse/hse.mjs`، `020`، `/hse`، `db/test/hse.mjs` |
| §47 EVM و ریسک | PV از نقاط S-curve برنامه‌ریز (درون‌یابی خطی)؛ EV یا از شمارش پلتفرم با قاعدهٔ اعتباردهی اعلام‌شده یا گزارشی با برچسب؛ AC از دفتر هزینه (اصلاح فقط با برگشت)؛ SPI/CPI/EAC/VAC/TCPI و هیچ نسبتی روی صفر؛ بدون ارز قرارداد، هیچ عدد پولی؛ رجیستر ریسک با P×I و نقشهٔ حرارتی ذاتی/باقیمانده | `lib/controls/evm.mjs`، `lib/controls/progress.mjs`، `021`، `037`، `/controls`، `db/test/controls.mjs`، `db/test/progress.mjs` |
| §19، C8–C9 Punch و NCR | Punch A/B/C روی ساب‌سیستم (از تگ مشتق)؛ رفع‌کننده ≠ تأییدکننده (در دیتابیس هم)؛ تغییر دسته رویداد با دلیل؛ NCR: دیسپوزیشن ← تأیید (ارفاق = امضای مهندسی، پیشنهاددهنده ≠ تأییدکننده) ← اجرا ← بستن فقط با علت ریشه‌ای و اقدام اصلاحی، اجراکننده ≠ بندنده؛ تشدید با مهلت اعلام‌شدهٔ پروژه | `lib/quality/quality.mjs`، `022`، `/quality`، `db/test/quality.mjs` |
| §20–21 Test Pack و MC | فشار تست B31.3 (هیدرو ۱٫۵·P·Rr، پنوماتیک ۱٫۱·P، ۱۰ دقیقه) فقط از فشار طراحی کلاس و Rr اعلام‌شده؛ آمادگی تست از جوش، NDT پذیرفته‌شدهٔ آخرین سیکل، PWHT و ساپورت؛ تست پذیرفته‌شده توسط نفر دوم؛ مرحلهٔ «تست» اسپول از پکیج؛ MC فقط با خالی بودن فهرست باز (تگ، خط، کابل، ابزار، Punch A، NCR)، امضا و پذیرش کارفرما دو نفر | `lib/completions/completions.mjs`، `023`، `/completions`، `db/test/completions.mjs` |
| §11، §29 خرید و Expediting، §12–13 VDRL | PO با ردیف کالای انبار یا تگ تجهیز (نه متن آزاد)؛ تأخیر نسبت به تعهد و شناوری نسبت به نیاز سایت (ROS) — بدون ROS «نامعلوم»؛ تاریخچهٔ پیش‌بینی با منبع؛ FAT منتظر مدارک «پیش از FAT» با کد ۱/۲، و حمل فقط با IRN؛ رسید انبار فقط در برابر ردیف PO، بدون رسید مازاد؛ کسری انبار حالا مقدار در راه و تاریخش را نشان می‌دهد؛ VDRL با کدهای ۱–۴ و مهلت ارسال مجدد اعلام‌شده | `lib/procurement/procurement.mjs`، `025`، `/procurement`، `db/test/procurement.mjs` |
| §11 MR و مناقصه، §12 Vendor Data Template | MR فقط در پیش‌نویس ویرایش می‌شود و صدور، رویژن با دلیل می‌سازد؛ پیشنهادِ رویژن قبلی، فنی‌نشده، منقضی یا بی‌قیمت رتبه نمی‌گیرد؛ رتبه فقط با یک ارز (نرخ تبدیل حدس زده نمی‌شود، وزن‌دهی پیش‌فرض نیست)؛ واگذاری را شخص انجام می‌دهد و غیرِ کمترین یا بدون رتبه، دلیل می‌خواهد؛ رتبه‌بندیِ لحظهٔ واگذاری نگه داشته می‌شود و PO از ردیف‌های رویژن صادرشده ساخته می‌شود. VDT: قالب هر کلاس ISO 14224 را پروژه تعریف می‌کند (با رویژن)؛ دادهٔ ناقص ثبت می‌شود ولی پذیرفته نمی‌شود؛ مقادیر پلاک پس از پذیرش به شناسنامهٔ نگهداری می‌رود و اختلاف با مقدار موجود نشان داده می‌شود، نه بازنویسی بی‌صدا | `lib/procurement/tender.mjs`، `038`، `/procurement` (دو زبانهٔ جدید)، `db/test/tender.mjs` |
| پایپینگ یکپارچه — داشبورد، NDT کامل، اینچ-قطر، Weld Log | یک نوار مشترک روی همهٔ صفحه‌های پایپینگ (ایزومتریک، اجرا، NDT، سابقهٔ جوش، بازرسی، پکیج تست، مواد)؛ داشبورد با جوش و اینچ-قطر (جوش بی‌سایز جدا شمرده می‌شود، نه صفر)، NDT کامل طبق الزام هر جوش با نمونهٔ جوشکار و بازرسی تدریجی B31.3 §341.3.4 با قرعهٔ ثبت‌شدهٔ جوش‌های اضافه (F-17، `042`)، خطوط با کلاس/الزام/قرعه/اسپول/پکیج، نرخ تعمیر جوشکار، اطلاعات ناقص، و Weld Log پروژه (CSV)؛ سابقهٔ جوش وضعیت الزام همان جوش را نشان می‌دهد | `lib/ndt/compliance.mjs`، `lib/db/repos/piping-hub.mjs`، `/piping/overview`، `db/test/ndt-compliance.mjs` |
| NDT جوش‌های سازه، ساپورت و تجهیزات (RT/UT/MT/PT/VT) | ماتریس NDT پروژه (دامنه × نوع اتصال × روش → درصد، با مرجع بند مشخصات؛ بدون درصد پیش‌فرض؛ RT/UT روی جوش گوشه رد)؛ رجیستر جوش روی تگ سازه/تجهیز یا ساپورت؛ هر جوش چند روش؛ ۱۰۰٪ با NDT خود جوش، درصد تصادفی با نمونهٔ seedدار جوشکار در گروه (قرعهٔ دوباره روی همان جمعیت همان نمونه را می‌دهد)؛ رد نمونه → «همهٔ لات» یا «تدریجی» طبق ماتریس، و بدون آن «نامعلوم»؛ مرحلهٔ مشتق «NDT جوش میدانی» در زنجیرهٔ سازه (پیش از رنگ و ضدحریق)، تجهیز ثابت و کوره (پیش از تست هیدرواستاتیک کویل)؛ بازرسی ساپورت تا NDT جوش اتصالش کامل نشود ثبت نمی‌شود | `lib/ndt/joints.mjs`، `043`، `/ndt-joints`، `db/test/joint-ndt.mjs` |
| کنترل مدارک — ترانسمیتال ورودی از طراح، IDC، CRS | ثبت یکجای ترانسمیتال طراح؛ هر ردیف یک رویژن با فایل (MinIO، SHA-256)؛ فایلِ عیناً تکراریِ رویژن قبل رد، مدرکِ خارج از MDR با برچسب، IFC پیش از تأیید ثبت و علامت‌دار (رسید، واقعیت است)؛ مهلت پاسخ = دریافت + مهلت قراردادی پروژه، بدون آن «نامعلوم»؛ توزیع بین رشته‌ها و معافیت فقط با دلیل؛ کامنت متعلق به مدرک است و تا ثبت پاسخ طراح باز می‌ماند؛ کد پاسخ را شخص می‌دهد و کد ۱ با کامنت باز، کد ۲ بدون کامنت، کد ۳ بدون کامنت باز و هر کد با بررسی باز رد می‌شود؛ خروجی CRS | `lib/documents/review.mjs`، `041`، `/documents` (زبانهٔ ورودی)، `db/test/incoming.mjs` |
| کدگذاری کالا — MESC (نفت، گاز و پتروشیمی ایران) | کد ده‌رقمی ۲·۲·۲·۳·۱ روی هر کالای انبار؛ کدها، عنوان گروه‌ها و واحد فقط از کاتالوگ MESC کارفرما (با نام ویرایش) — پلتفرم کد نمی‌سازد؛ بدون کاتالوگ «بررسی‌نشده»، کد خارج از کاتالوگ یا با واحد دیگر رد می‌شود؛ یک کد = یک کالا (تکراری رد)؛ جست‌وجو در کاتالوگ برای انتخاب انسانی؛ بارگذاری کاتالوگ همه یا هیچ | `lib/warehouse/mesc.mjs`، `039`، `/warehouse`، `db/test/mesc.mjs`، `docs/mesc-catalogue-template.csv` |
| §8 کنترل مدارک | رجیستر MDR، رویژن با هدف صدور (IFR/IFA/IFC/AB/VOID) و بدون تاریخ‌گذاری به عقب؛ IFC مدرکِ نیازمند تأیید فقط بعد از کد ۱/۲ کارفرما؛ «رویژن جاری» و «رویژن ساخت» در هر تاریخ؛ ترانسمیتال با پاسخ کارفرما؛ نقشهٔ بارگذاری‌شده‌ای که رویژن ساختِ روز خودش نبوده پرچم می‌خورد | `lib/documents/control.mjs`، `026`، `/documents`، `db/test/doc-control.mjs` |
| §24–27، §43–44 تحویل به CMMS | شناسنامهٔ نگهداری هر تگ تجهیز: کلاس ISO 14224 (انتخاب مهندس، نه حدس از پیشوند)، Criticality فقط از سطوح اعلام‌شدهٔ شرکت و با مبنا، سازنده/مدل/سریال با تاریخچهٔ تغییر؛ Functional Location از الگوی پروژه؛ چک‌لیست تحویل (MC پذیرفته، IOM تأییدشده در VDRL) که «نامعلوم» را هم مانع می‌داند؛ خروجی CSV خنثی با هش و ثبت؛ **مرحلهٔ ۲:** برنامهٔ PM (زمانی، وضعیت‌محور، تا خرابی) که هر بازه‌اش منبع دارد (سازنده، RCM، قانونی، استاندارد شرکت) و پلتفرم هیچ بازه‌ای پیشنهاد نمی‌دهد؛ تهیه‌کننده ≠ تأییدکننده، تسک تأییدشده ثابت و تغییرش رویژن (در دیتابیس هم)؛ قطعات یدکی SPIR (راه‌اندازی، بهره‌برداری، سرمایه‌ای) متصل به کالای انبار با تخصیص موجودی قابل حواله و Interchangeability خوانده‌شده از ردیف‌ها؛ کالیبراسیون دوره‌ای با سررسید از آخرین کالیبراسیون **قبول‌شده**؛ «برنامهٔ PM تأییدشده» جزو چک‌لیست تحویل؛ خروجی CSV برنامه و قطعات با هش | `lib/cmms/handover.mjs`، `027`، `/handover`، `db/test/handover.mjs`، `036`، `lib/cmms/maintenance.mjs`، `db/test/maintenance.mjs` |
| §15، §17، C11 QR و PWA آفلاین | برچسب QR با لینک معمولی صفحهٔ سایت (دوربین هر گوشی بازش می‌کند؛ سطح تصحیح Q؛ رفت‌وبرگشت با خوانندهٔ مستقل jsQR تست شده)؛ اسکنر داخل برنامه (BarcodeDetector یا jsQR)؛ Service Worker که فقط برنامه را کش می‌کند نه داده را؛ «بستهٔ سایت» در IndexedDB با زمان تولید؛ ثبت مرحلهٔ تگ و اسپول و ثبت/رفع Punch بدون شبکه، در صف با شناسهٔ یکتا؛ همگام‌سازی idempotent که هر عملیات را در تراکنش خودش با همان موتورها اعمال می‌کند و ردها را با دلیل نگه می‌دارد؛ عکس سایت پیوست Punch (آفلاین، فشرده و بدون EXIF روی گوشی، نوع از محتوای فایل، append-only با هش) | `lib/field/`، `lib/quality/photo.mjs`، `028`، `029`، `/field`، `/labels`، `/api/quality/photo`، `public/sw.js`، `db/test/field.mjs`، `tools/drive-field*.mjs` |
| §18 بازرسی: ITP و IR | ITP سند کنترل‌شده با رویژن: تهیه‌کننده ≠ تأییدکننده، پس از تأیید ثابت (در دیتابیس هم)، رویژن جدید قبلی را منسوخ می‌کند؛ نقاط H/W/R/S برای پیمانکار، کارفرما و TPI؛ هر ردیف می‌تواند به یک مرحلهٔ زنجیره گره بخورد و آن مرحله در همهٔ رشته‌ها (تگ، فونداسیون، سازه، اسپول، کابل، ابزار، رنگ) تا آزاد شدن بازرسی ثبت نمی‌شود — آنلاین و آفلاین؛ وضعیت درخواست (منتظر/آزاد/رد) ذخیره نمی‌شود و از نتیجه‌ها خوانده می‌شود؛ Hold فقط با پذیرش همان طرف آزاد می‌شود، Witness با پذیرش یا غیبت پس از اطلاع‌رسانی کافی — و بدون مدت اعلام‌شدهٔ پروژه غیبت چیزی را آزاد نمی‌کند؛ طرف امضاکننده از عضویت (نه نقش)؛ درخواست‌دهنده به‌جای کارفرما امضا نمی‌کند؛ رد با دلیل، بازرسی دوباره و NCR متصل؛ نتیجه append-only؛ بازرسی انجام‌شده زیر رویژن قبلی برای همان ردیف معتبر می‌ماند. **بازرسی رسید کالا (MIR) هم زیر ITP است:** لات انبار آیتم بازرسی است و ITP «دریافت کالا» دو مرحلهٔ خود انبار را نام می‌برد (MIR و بررسی MTC)؛ پذیرش لات یا MTC آن تا آزادشدن IR ثبت نمی‌شود، رد کردن هرگز منتظر نمی‌ماند، و لاتی که پیش از ITP بازرسی شده دست نمی‌خورد | `lib/inspection/itp.mjs`، `030`، `034`، `/inspection`، `/warehouse`، `db/test/inspection.mjs`، `db/test/material-inspection.mjs` |
| §22 پیش‌راه‌اندازی و RFSU | پروژه چک‌لیست‌هایش را اعلام می‌کند و هرکدام به یک نوع آیتم (ساب‌سیستم، تجهیز دوّار/ثابت/آتشین، خط، کابل، ابزار، لوپ) اعمال می‌شود — نه حدس پلتفرم؛ ثبت فقط پس از پذیرش MC؛ رد با شرح؛ قبول پس از پذیرش نفر دوم (در دیتابیس هم)؛ آخرین رکورد تصمیم می‌گیرد و فقط آخرین پذیرفته می‌شود؛ RFSU فقط با MC پذیرفته، همهٔ چک‌لیست‌ها پذیرفته، بدون Punch A/B و NCR باز — و بدون هیچ چک‌لیست اعلام‌شده امضا نمی‌شود؛ امضاکننده ≠ پذیرنده؛ آنچه تأیید شده با امضا نگه داشته می‌شود و بازشدن بعدی روی برد دیده می‌شود | `lib/completions/precom.mjs`، `032`، `/precom`، `db/test/precom.mjs` |
| §33 احراز هویت و نقش‌ها | ورود، نقش‌ها، RLS جداسازی پروژه در خود دیتابیس؛ توسعه‌دهندهٔ سامانه (پرچم سطح پلتفرم، فقط از خط فرمان یا توسعه‌دهندهٔ دیگر)؛ حوزهٔ کاری هر عضو که منو و مجوز ثبت را در سرور محدود می‌کند؛ دعوت با لینک یک‌بارمصرف (هش‌شده، ۷۲ ساعت)؛ غیرفعال‌سازی حساب؛ حداقل یک مدیر در هر پروژه | `010`، `033`، `/admin`، `/setup`، `db/test/auth.mjs`، `db/test/users.mjs` |
| §37 Power BI | لایهٔ `reporting.*` با `security_invoker`؛ `fact_progress` چندرشته‌ای (جوش، کابل، ابزار) و `kpi_subsystem_readiness` | `006`، `015`، `016` |
| §65 No-Hallucination | هر موتور در نبود داده حکم نمی‌دهد (F-10) و دلیلش را می‌گوید | `docs/epc-llm/04-engine-findings.md` |

## ۲. نیمه‌کاره

| بخش Master Plan | وضعیت | کمبود |
|---|---|---|
| §4 Asset Master | تگ، کلاس ISO 14224، Criticality، سازنده/مدل/سریال، Functional Location | شرایط طراحی (فشار/دما از دیتاشیت)، شناسه‌های DCS/Historian، ابزارها به‌عنوان asset |
| §8 Document Control / CDE | رجیستر، رویژن، ترانسمیتال، رویژن معتبر در تاریخ (بالا) | ذخیرهٔ فایل هر رویژن (MinIO)، گردش کار بررسی داخلی (IDC) |
| §22 Commissioning | آمادگی ساب‌سیستم، Test Pack، گواهی MC، **چک‌لیست پیش‌راه‌اندازی و RFSU (بالا)** | عملیات راه‌اندازی (پس از RFSU)، RFC جدا، Performance Test |
| §18 QA/QC | NDT، رد/قبول موتورها، Punch و NCR، **ITP و درخواست بازرسی (بالا)** | برنامهٔ نمونه‌برداری بازرسی کارفرما؛ ITP رسید کالا هنوز برای همهٔ دسته‌های کالا یکی است (ITP جدا برای مواد تحت فشار و برای الکتریکال نه) |
| §47 EVM | ساخته شد (بالا) | EV پلتفرم برای شش رشته (جوش، کابل، ابزار، فونداسیون، سازه، رنگ). تاریخچه فقط از زمانی است که اسنپ‌شات ماهانه ثبت شود — ماه‌های قبل از آن بازسازی نمی‌شوند. وزن‌دهی سلسله‌مراتبی WBS بالاتر از حساب کنترلی نیست |
| HSE | ساخته شد (بالا) | JSA/TRA، بازرسی داربست و جرثقیل، آموزش و کارت صلاحیت افراد، گزارش ماهانهٔ IOGP |
| انبار | ساخته شد (بالا)، به‌علاوهٔ رزرو، انبارگردانی و MESC (`040`، `039`، `lib/warehouse/reserve.mjs`، `db/test/reserve.mjs`) | رزرو خودکار از روی برنامهٔ ساخت اسپول (فعلاً رزرو را شخص ثبت می‌کند)؛ ارزش ریالی موجودی و مغایرت |

## ۳. شروع نشده (به ترتیب پیشنهادی Master Plan)

1. ~~Assumption Register + Missing Information (§2.51–2.53)~~ — **ساخته شد**:
   `018_assumptions.sql`، `lib/db/repos/assumptions.mjs`، `/assumptions`، `db/test/assumptions.mjs`.
   فرض با رویژن و تصمیم امضاشده؛ تاریخچه append-only در سطح دیتابیس؛ اطلاعات ناقص از همان
   داده‌ای حساب می‌شود که موتورها با آن حکم می‌دهند.
2. ~~صفحهٔ Asset / Digital Readiness (§60، §79)~~ — **ساخته شد**: `lib/db/repos/asset.mjs`، `/asset?tag=`،
   `db/test/asset.mjs`. امتیاز فقط روی «هویت» (ساب‌سیستم، نوع، شرح)؛ حوزهٔ بی‌داده «ثبت نشده» است نه
   «ناقص»، و حوزه‌هایی که ماژولشان ساخته نشده (Vendor، Procurement، CMMS، DCS) جدا نام برده می‌شوند.
3. ~~Punch و NCR با تشدید خودکار (§19، C8–C9، C26)~~ — **ساخته شد** (بالا).
4. ~~QR برای تگ، اسپول، کابل، ابزار (§17) و PWA آفلاین برای سایت (§15، C11)~~ — **ساخته شد**؛ ثبت آفلاین کابل (مرحله، IR) و ابزار (مرحله، کالیبراسیون، لوپ چک) هم ساخته شد، با پیش‌داوری روی گوشی از همان موتور؛ عکس سایت پیوست Punch هم ساخته شد.
5. ~~Test Pack و MC Package خودکار از پیش‌نیازها (§20–21، C19–C21)~~ — **ساخته شد**؛ RFC/RFSU و پیش‌راه‌اندازی هنوز نه.
6. ~~Procurement: PO → FAT → حمل → رسید سایت (§11، §29) و VDRL (§12–13)~~ — **ساخته شد**؛ MR، مناقصه و Vendor Data Template هم ساخته شد (۲۰۲۶-۰۹-۲۶). هنوز نه: پرتال وندور (ورود دادهٔ VDT توسط خود وندور)، ارزیابی وزنی با سیاست مناقصهٔ شرکت.
7. ~~Material Traceability: Heat Number / MTC تا اسپول (§14)~~ — **ساخته شد** (انبار)؛ تا سطح تک‌جوش هنوز نه.
8. ~~Handover به CMMS با ساختار ISO 14224 (§24–27، §43–44)~~ — **ساخته شد** (شناسنامه، FLOC، خروجی)؛ برنامهٔ PM، قطعات یدکی (SPIR) و کالیبراسیون دوره‌ای هم ساخته شد (مرحلهٔ ۲)؛ ثبت دستور کار و سابقهٔ خرابی (بهره‌برداری) در CMMS خود کارفرما می‌ماند.
9. خواندن P&ID و SLD (C-32، C-33) — همان الگوی ایزومتریک: مدل می‌خواند، موتور تصمیم می‌گیرد، انسان امضا می‌کند.
10. خاکبرداری، داربست (C-37 تا C-40) — «Preliminary / Calculated / Survey-Verified» جدا.
11. Historian/OPC UA، انرژی، Production Accounting، ذخیره و صادرات (§24–38) — فاز بهره‌برداری.
12. RAG با قاعدهٔ «بدون منبع، بدون جواب» (§16–17، §41).

## ۴. تفاوت فناوری با Master Plan — آگاهانه

Master Plan پشتهٔ FastAPI + React + n8n + MinIO + Qdrant را پیشنهاد می‌کند. این
پلتفرم روی Next.js (Node) + PostgreSQL ساخته شده است. طبق قاعدهٔ خود Master
Plan («سیستم موجود اول»، «بدون بازنویسی بی‌دلیل») بازنویسی نمی‌شود:

- **PostgreSQL مرکزی** — همان تصمیم Master Plan؛ با RLS برای جداسازی پروژه.
- **REST API** — همهٔ ماژول‌ها از `app/api/*` در دسترس‌اند؛ n8n می‌تواند
  مستقیم آنها را صدا بزند (Webhook / HTTP Request) بدون تغییر معماری.
- **Python** جایی اضافه می‌شود که واقعاً بهتر است: پردازش نقشه (Vector/OCR)،
  یادگیری ماشین نگهداری پیشگویانه — به‌صورت سرویس جدا کنار همین دیتابیس.
- **MinIO** وصل شد (`STORAGE_DRIVER=s3`): نقشه‌ها و عکس‌های Punch در bucket، با کلید هش
  محتوا و کنترل هش در ورود و خروج؛ بارگذاری مستقیم نقشهٔ بزرگ از مرورگر با URL امضاشده برای
  همان هش؛ کاربر برنامه بدون مجوز حذف (`deploy/minio/`)؛ ابزار `npm run storage` برای
  راه‌اندازی، انتقال از دیسک و بررسی. فایل‌های بزرگ هرگز در PostgreSQL ذخیره نمی‌شوند (قاعدهٔ §48).

## ۵. آنچه عمداً انجام نمی‌شود (§62)

Digital Twin گران پیش از Asset Master؛ AI پیش از کیفیت داده؛ اتصال هر چیز به
DCS/SIS؛ Agent با اجازهٔ نوشتن روی داده‌های مهندسی بدون تأیید انسانی.
